Modules Partie Processing

	- proc_cmd		(processing commande) -> lien couche réseau
	  -> (none)		(appel des pointeur sur fonction)
	- msgout_cmd		(protocole de sortie)
	  -> (none)		(fonctions de protocole qui vont realiser les messages de sorties)

	- func_action		(fonctions d'action)
	  -> (none)		(toutes les fonctions des actions appelées par proc_cmd ; passer is_processing a FALSE)

	- handle_map		(gestion de la map)
	  -> (none)		(gere les ressources disposées aléatoirement dans la map)
	- handle_object		(gestion des objets)
	  -> (none)		(création, transfert)
	- handle_move		(gestion des déplacements)
	  -> (none)		(les déplacements sont circulaires)
	- handle_victory	(gestion des conditions de victoire)
	  -> (none)		(qui le jeu -> ferme toutes les connections ; ?? fermeture du serveur ??)
